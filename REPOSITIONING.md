> **ملاحظة:** هذه وثيقة مرحلة سابقة (الموقع كمرجع تعليمي أولاً). النموذج الحالي: المتجر أولاً، والخبرة خدمة إضافية مدفوعة — راجع README.md.

# إعادة التموضع — من متجر لحوم إلى مرجع اللحوم
## Repositioning Brief & Experience Architecture
**الحالة:** مقترح بانتظار الاعتماد — لم يُكتب أي كود بعد.
**التاريخ:** 2026-09-20

---

# PHASE 01 — AUDIT (الوضع الحالي)

## 1.1 ما هو موجود فعلياً

| | |
|---|---|
| الصفحات | 6 (`index`, `category`, `product`, `checkout`, `wishlist`, `account`) |
| CSS | ملف واحد، 599 سطر، ~130 كلاس |
| JS | ملف واحد، 356 سطر، مشترك بين كل الصفحات |
| الصور | 53 ملف، 5.9 ميجا |
| التاريخ | 7 كوميتات — تطوّر من واير فريم رمادي إلى متجر بصور |

## 1.2 نقاط القوة (نحتفظ بها)

1. **محرك سلة يعمل فعلاً** — `addItem / changeQty / removeItem / renderCart` + `localStorage` + مزامنة عبر الصفحات الست. ده أصل حقيقي، مش نموذج.
2. **Shell موحّد** — header / drawer / mobile nav / bottom tab bar / toast، مكرّر بثبات في كل الصفحات.
3. **RTL عربي سليم** من الأساس — مش ترجمة فوق تصميم إنجليزي.
4. **نظام responsive ناضج** — 4 breakpoints، شيلفات أفقية للموبايل، فلتر منزلق، tab bar سفلي.
5. **طبقة بيانات المنتج** — `cardHTML` مركزي، فـ أي تغيير في شكل المنتج بيتم في مكان واحد.
6. **أداء ممتاز** — HTML/CSS/JS خام، بدون أي إطار عمل أو تبعيات.

## 1.3 المشاكل الحقيقية (يجب حلّها)

### أ. المشكلة الاستراتيجية — الموقع بلا موقف
الهيكل الحالي هو **Discover → Price → Add to Cart**. كل سيكشن فيه ممكن ينتقل حرفياً لأي متجر تاني: عدّاد عروض، شيلفات منتجات، بانرات ترويجية، تيكر "آخر عملية شراء"، شبكة فيديوهات. طبّقنا اختبار §26 سؤال 6 (هل ممكن السيكشن ده يبقى على أي متجر عشوائي؟) على الهوم بيدج:

| السيكشن | ممكن يكون على أي متجر؟ | القرار |
|---|---|---|
| هيرو عرض الأسبوع + عدّاد | ✅ نعم | **REBUILD** |
| شريط المميزات | ✅ نعم | MODIFY (نحتفظ، نعيد صياغته) |
| دوائر التصنيفات | ✅ نعم | REBUILD (تصير خريطة قطعيات) |
| تبويبات الأكثر مبيعاً/تخفيضاً | ✅ نعم | **REMOVE** من الهوم |
| بانرات ترويجية ×3 | ✅ نعم | **REMOVE** |
| شيلفات المنتجات | ✅ نعم | MODIFY (تتحول لرفّ "قطعيات" بمواصفات) |
| آراء العملاء | ✅ نعم | KEEP (منخفض الأولوية) |
| شبكة الفيديوهات | ✅ نعم | REBUILD (تصير تقنيات طبخ) |
| تيكر آخر عملية شراء | ✅ نعم | **REMOVE** |

**النتيجة: 9 من 11 سيكشن فشلوا في الاختبار.** ده مش موقع محتاج تحسين — ده موقع محتاج معمارية جديدة.

### ب. مشكلة ملكية الأصول البصرية ⚠️ (الأخطر)
كل الصور الحالية **مأخوذة من منافسين مباشرين**:
- 5 بانرات من `kabshnajd.com` — وفيها **نصوصهم وعروضهم وزخرفتهم محروقة داخل الصورة** ("395"، "699"، "التوفير الكبير"). علشان كده اضطرينا نحط لوحة داكنة فوقها عشان نصنا يتقرا — ده عرض للمشكلة مش حل.
- 48 صورة منتجات من `turkidabayeh.com` — وفيها **كراتين تركي المطبوعة بعلامتهم** ظاهرة في أغلب كروت المنتجات.

علامة بتدّعي **المرجعية والخبرة** لا يمكن تُبنى على مواد تسويقية لمنافس. ده سقف فني قبل ما يكون مشكلة قانونية: مستحيل نعمل art direction أصلية على صور بتنتمي لهوية تانية. **قرار مطلوب — بند 1 في آخر المستند.**

### ج. الملاحة تقتل الـ SEO
حالياً **صفر عنصر `<a href>`** في الموقع كله — كل التنقّل عبر `data-nav` + `location.href` في JS. ده كان قرار صح وقت الواير فريم، لكنه دلوقتي:
- محركات البحث ما تقدرش تزحف بين الصفحات (لا روابط = لا اكتشاف = لا ترتيب)
- مساعدات الـ AI ما تقدرش تقرأ بنية الموقع (يتعارض مباشرة مع مطلب §14: دعم SEO و GEO)
- لا فتح في تاب جديد، لا مشاركة رابط من كارت منتج، لا زر رجوع منطقي

**قرار: نرجّع روابط `<a href>` دلالية في كل مكان.** ده شرط أساسي لمطلب المحتوى والبحث، مش تفضيل.

### د. النظام البصري "قالب"
- الخلفية `#f7f8fa` رمادي بارد = لغة SaaS/dashboard، مش لغة طعام أو تحرير.
- الأحمر `#c1121f` أحمر تجاري صريح — بالظبط الـ "أحمر + أسود" اللي البريف (§07) حذّر منه.
- كل شيء مستدير `12–18px` — الكروت والبانرات والأزرار بنفس النعومة = لا يوجد تسلسل أو نبرة.
- **التقييم بالنجوم** على قطعة لحم لا يعني شيئاً معرفياً. النجوم تقيس رضا، مش تقيس قطعة.
- لا يوجد تايبوغرافي display — كل العناوين بنفس العائلة والوزن تقريباً.

### هـ. صفحة المنتج تبيع قبل ما تشرح
الترتيب الحالي: صورة ← سعر ← خيارات ← أضف للسلة ← (وبعدين) وصف ومواصفات.
ده يخلّي المنتج سلعة يتقارن سعرها. البريف §13 يطلب العكس تماماً.

### و. مشاكل أصغر مرصودة
- `cardHTML` يولّد `★★★★★` عشوائياً — أرقام غير حقيقية تُعرض كأنها تقييمات (يخالف §22).
- `catSort` يرتّب لكن الفلاتر الجانبية **زخرفية** (`.fopt` toggle بصري فقط).
- تصنيفات المنيو وشيبس التنقّل تودّي كلها لنفس `category.html` بدون فلترة فعلية.
- `.ph` (أيقونة الـ placeholder الرمادية) لسه ظاهرة في الخريطة وأفاتار الحساب والتقييمات — بقايا واير فريم في موقع "حقيقي".

## 1.4 جدول القرار — المكوّنات الـ 130

**KEEP (نحتفظ كما هو):** محرك السلة، `drawer`، `mnav`، `tabbar`، `toast`، `overlay`، `qty`، `field`، `form-grid`، `card-box`، `radio-opt`، `summary`، `steps`، `otable`، `acc-*`، `crumbs`، `pager` — كل طبقة الحساب والدفع سليمة وظيفياً ومحتاجة إعادة تلبيس بصري فقط.

**MODIFY (نحتفظ بالمنطق، نغيّر اللغة البصرية):** `header`، `search`، `tool`، `footer`، `trust`، `btn`، `chip`، `section`، `sec-head`، `wrap`، `pcard` (إعادة تصميم جذرية للمحتوى الداخلي)، `pgrid`، `shelf`، `filter` (+ تفعيل حقيقي).

**REBUILD (نفس الغرض، تنفيذ جديد بالكامل):** `hero-banner` → بيان مرجعية + مدخل الخريطة، `tiles` → خريطة القطعيات، `pdp` → صفحة قطعة تعليمية، `videos` → تقنيات، `reviews` → إعادة تلبيس.

**REMOVE (يُحذف):** `cd` (العدّاد)، `ticker`، `fbanner`، `banner/banners`، `ptabs` من الهوم، `stats`، `tag-sale`/`tag-off` كبطل بصري، `stars` كمقياس للقطعة، `ph-ico` (أيقونة الواير فريم).

---

# PHASE 02 — المعمارية الجديدة

## 2.1 الفكرة المركزية

> ## اعرف القطعة. أتقن الطبخة.
> **KNOW THE CUT. MASTER THE COOK.**

المفهوم التنفيذي: **الموقع مبني كـ "فهرس مرجعي" لا كـ "كتالوج".**
كل قطعة لحم تُعامَل كـ **مُدخَل في مرجع**: لها كود، وموقع تشريحي، وورقة مواصفات، ومنهج طبخ، ودرجة نضج موصى بها — وبعدين، وبعدين بس، يقدر تشتريها.

ده اللي يخلّي المقارنة مع ملحمة عادية مستحيلة: هم يبيعوا كيلو، احنا نبيع **قرار صح**.

## 2.2 الملاحة (5 عناصر، مبنية على النية)

| العنصر | النية | المحتوى |
|---|---|---|
| **وش تطبخ؟** | المبتدئ عنده طبق في باله | دخول حسب الطبق/المناسبة → يقترح القطعة |
| **دليل القطعيات** | يريد يفهم | خريطة تشريحية + فهرس كل قطعة |
| **مختبر الطبخ** | يملك القطعة ويريد ينجح | طرق، حرارات، درجات نضج، أخطاء شائعة |
| **المتجر** | يريد يشتري | تجارة كاملة |
| **اسأل الجزّار** | محتار ويريد توجيه | استشارة موجّهة بأسئلة |

خمسة. مش أكتر. (§16: الفخامة ليست تعقيداً.)

## 2.3 خريطة الموقع المقترحة

```
/                          الرئيسية — بيان المرجعية
/cuts/                     دليل القطعيات (خريطة تشريحية تفاعلية)
   /cuts/[كود]/            صفحة القطعة — تعليمية أولاً، تجارية ثانياً
   /cuts/compare/          مقارنة قطعتين جنباً إلى جنب
/cook/                     مختبر الطبخ
   /cook/[طريقة]/          شوي · قلي · تحمير عكسي · طبخ بطيء · فرن
   /cook/doneness/         مرجع درجات النضج والحرارات
/what-to-cook/             دخول حسب الطبق/المناسبة
   /what-to-cook/[طبق]/    كبسة · مندي · ستيك · برجر · ولائم · مشاوي
/consult/                  اسأل الجزّار — استشارة موجّهة
/cutting-room/             من الذبيحة إلى القطعة (قصة سكرول تحريرية)
/library/                  المكتبة — مقالات وأدلة (SEO/GEO)
   /library/[مقال]/
/shop/                     المتجر — الفهرس التجاري
   /shop/[منتج]/           شراء (يرث تعليم القطعة)
/cart · /checkout · /account · /wishlist    (قائم — إعادة تلبيس فقط)
```

## 2.4 معمارية الصفحة الرئيسية

ترتيب مقصود: **سلطة → توجيه → معرفة → منتج**. لا يوجد سعر فوق الطية.

| # | السيكشن | الوظيفة | لماذا ليس عاماً |
|---|---|---|---|
| 01 | **بيان المرجعية** | "اعرف قطعتك" + مدخل مباشر للخريطة | ليس بانر عرض — هو تصريح موقف |
| 02 | **وش تطبخ اليوم؟** | 6 مداخل حسب الطبق | يبدأ من نية المستخدم لا من الكتالوج |
| 03 | **خريطة القطعيات** ★ | خريطة تشريحية تفاعلية | التجربة المميّزة — لا توجد عند أي منافس |
| 04 | **قطعة الأسبوع** | قطعة واحدة بعرض تحريري كامل | عمق بدل عرض عشرة منتجات |
| 05 | **مختبر الطبخ** | 3 طرق + مقياس الحرارة | مكوّن وظيفي مملوك للعلامة |
| 06 | **من الذبيحة إلى القطعة** | قصة سكرول | يشرح المنشأ = يبني الثقة |
| 07 | **اسأل الجزّار** | مدخل الاستشارة | خدمة، مش عرض |
| 08 | **المكتبة** | 3 مقالات | وقود SEO/GEO |
| 09 | **رفّ الطلب** | تجارة أخيراً | يظهر بعد المعرفة |
| 10 | المميزات + الفوتر | ثقة وخدمة | إعادة تلبيس للقائم |

## 2.5 صفحة القطعة (PDP) — الترتيب الجديد

```
اسم القطعة  +  الكود  +  الاسم الإنجليزي
        ↓
من أين تأتي؟          ← مُحدِّد موقع مصغّر على الخريطة
        ↓
ورقة المواصفات        ← طراوة · دهن · قوة النكهة · صعوبة  (أشرطة قياس، لا نجوم)
        ↓
أفضل لـ               ← الأطباق المناسبة
        ↓
التحضير               ← تتبيل · تجفيف · وصول لحرارة الغرفة
        ↓
الطبخ                 ← الطرق + الأزمنة
        ↓
درجة النضج            ← مسطرة حرارة تفاعلية
        ↓
وصفات مرتبطة
        ↓
احصل على القطعة       ← وزن · تقطيع · كمية · أضف للسلة   (ثابت أسفل الموبايل)
        ↓
قطع قريبة + قارن
```

## 2.6 رحلة التعليم ← التجارة (محرّك التحويل)

```
مدخل: بحث "أفضل لحم للكبسة" أو طبق من الهوم
   ↓
صفحة الطبق: الكبسة → القطع الموصى بها + لماذا
   ↓
صفحة القطعة: من أين · مواصفات · لماذا مناسبة للكبسة
   ↓
مختبر الطبخ: طريقة + حرارة + زمن
   ↓
ثقة مكتسبة
   ↓
"احصل على هذه القطعة" ← وزن مقترح لعدد الأشخاص
   ↓
السلة (قائمة وتعمل)
```

كل صفحة تعليمية لازم تنتهي بـ **"احصل على هذه القطعة"**، وكل صفحة منتج لازم ترجع لـ **"اعرف هذه القطعة"**. الربط ثنائي الاتجاه — ده النظام، مش صفحات منفصلة.

---

# PHASE 03 — النظام البصري

## 3.1 اللون — ورق الجزّار، لا الأحمر والأسود

| الدور | اللون | لماذا |
|---|---|---|
| الأساس | `#F4F1EA` ورق جزّار دافئ | يقتل إحساس القالب فوراً — دافئ لا رمادي بارد |
| الحبر | `#1E1A18` فحمي بنّي | ليس أسود نقي — ألين وأغلى |
| الأساسي | `#6B1D24` أوكس-بلَد | عنّابي عميق، ليس أحمر إطفاء |
| إشارة حيّة | `#D9615E` وردي لحم نيء | **للبيانات فقط**: مقياس الدهن، مسطرة النضج |
| فولاذ | `#8A8F94` | التعليقات التقنية والأكواد |
| سطح | `#FFFFFF` | بطاقات المواصفات فقط |

الوردي مش زخرفة — هو **لون وظيفي** يعني "بيانات قطعة". ده اللي يمنع الوقوع في §07.

## 3.2 التايبوغرافي — ثلاث طبقات بوظائف مختلفة

| الطبقة | الخط | الاستخدام |
|---|---|---|
| **Display** | Noto Kufi Arabic 800/900 | عناوين ضخمة، تتبّع مضغوط — الصوت التحريري |
| **Body/UI** | IBM Plex Sans Arabic (موجود) | نصوص وواجهة — محايد وتقني |
| **Technical** | IBM Plex Mono + أرقام جدولية | الأكواد، الحرارات، الأوزان، النسب |

الطبقة التقنية هي التوقيع: لما تشوف `٥٢°م` و `ض–04` بخط monospace، الموقع يقول "أنا مرجع" بدون ما يكتبها.

## 3.3 المكوّنات المملوكة (التي تصنع الفرق)

1. **CUT MAP** — خريطة SVG تشريحية، المناطق الأولية قابلة للاختيار، تكبير للقطعة.
2. **SPEC METER** — 4 أشرطة قياس (طراوة/دهن/قوة/صعوبة) تحل محل النجوم نهائياً.
3. **DONENESS RULER** — مسطرة حرارة تفاعلية بالدرجات المئوية، من نيء إلى ناضج تماماً.
4. **CUT CODE** — رمز فهرسي لكل قطعة، بخط monospace، يعمل كـ SKU ومعرّف بصري.
5. **CUTTING LINE** — فاصل بصري بعلامات تسجيل (registration marks) بدل الخط الرمادي العادي.
6. **ORIGIN LOCATOR** — خريطة مصغّرة تُظهر موقع القطعة، تتكرر في كل مكان تُذكر فيه القطعة.

## 3.4 الحركة — تشرح، لا تزيّن

| التفاعل | ما يشرحه |
|---|---|
| تحوّل الخريطة: حيوان → منطقة → قطعة | العلاقة التشريحية |
| ملء مسطرة النضج مع تغيّر الحرارة | أثر الحرارة على النتيجة |
| تعبئة أشرطة المواصفات عند الظهور | مقارنة القطع |
| سكرول غرفة التقطيع | التحوّل من ذبيحة لقطعة |

ممنوع: parallax عشوائي، ظهور بطيء لكل عنصر، حركة بلا معنى.

## 3.5 الموبايل (أولوية أولى)

- شريط شراء ثابت أسفل صفحة القطعة (السعر + أضف للسلة) طول السكرول.
- الخريطة التشريحية على الموبايل = **قائمة مناطق قابلة للسحب** لا صورة مصغّرة (إعادة تصميم للتفاعل، لا تصغير).
- كتل معرفية قصيرة قابلة للمسح — لا فقرات طويلة.
- الـ tab bar السفلي قائم ويعمل — نحتفظ به.

---

# PHASE 04 — ترتيب التنفيذ المقترح

| الموجة | المخرج | لماذا الترتيب ده |
|---|---|---|
| **1** | نظام التصميم + الـ Shell (لون، تايبو، هيدر، فوتر، روابط `<a>` حقيقية) | كل شيء بعده يرث منه |
| **2** | خريطة القطعيات + صفحة القطعة | قلب التموضع — لو ده نجح، الباقي سهل |
| **3** | الهوم بيدج بالمعمارية الجديدة | يعتمد على 1 و 2 |
| **4** | مختبر الطبخ + وش تطبخ؟ | طبقة المعرفة |
| **5** | ربط التعليم بالتجارة + المكتبة + SEO/GEO | الشبكة والنمو |
| **6** | إعادة تلبيس الدفع/الحساب/المفضلة | قائم ويعمل — آخر أولوية |

---

# قرارات مطلوبة منك قبل البدء

### 1. الصور ⚠️ (الأهم)
الصور الحالية ملك kabshnajd و turkidabayeh، وفيها نصوصهم وكراتينهم. الخيارات:
- **أ)** نولّد صور أصلية للعلامة (أفضل نتيجة فنياً، وتحرّر الـ art direction بالكامل)
- **ب)** نستخدم قصّات محايدة من الصور الحالية (لحم فقط، بدون كراتين أو نصوص) كمرحلة انتقالية
- **ج)** نبقيها كما هي بوضوح كـ placeholder لحد ما تصوّر جلسة حقيقية

### 2. اسم العلامة
الموقع حالياً بلا اسم (دايرة حمرا). المرجعية تحتاج صوت له اسم. أقترح نشتغل بـ `[العلامة]` كـ placeholder محدّد في النظام، وتعطيني الاسم لما يجهز — أو تعطيني الاسم الآن وأبني التايبوغرافي حواليه.

### 3. اللغة
البريف يطلب العربية كلغة أولى (وده الحاصل). هل نبني **ثنائي AR/EN من البداية** — أم **عربي أولاً** وننجز الإنجليزي كموجة لاحقة؟

### 4. نطاق الموجة الأولى
أبدأ بـ **الموجة 1 + 2** (النظام + الخريطة + صفحة القطعة) عشان تشوف التموضع شغّال على أرض الواقع قبل ما نلمس باقي الموقع — أم تفضّل ترتيب مختلف؟

---

**ملاحظة على المحتوى:** التزاماً بـ §22 — لن يتم اختراع شهادات، جوائز، أرقام عملاء، أو ادعاءات منشأ. أي بيانات قطعيات (حرارات، درجات نضج، طرق طبخ) هي معرفة طهي عامة قابلة للتحقق. أي رقم خاص بالعمل يُكتب كـ placeholder واضح.
